/**
 * Fixed news category catalog. Drives the admin category toggles and the
 * public filter chips (stable order matching the design).
 */
export const NEWS_CATEGORIES = ['Media Articles', 'Press Kit', 'Events'] as const;
